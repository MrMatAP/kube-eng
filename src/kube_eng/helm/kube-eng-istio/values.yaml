# Default values for Kube Eng :: Istio

ambient: true
classic_gateway: false

monitoring:
  enabled: false

overrides_istiod:
  defaultRevision: default
  profile: ambient

overrides_cni:
  profile: ambient

