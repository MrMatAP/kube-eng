#
# Default values for Kube Eng :: Keycloak Operator

cluster:
  name: cluster
  edge:
    kind: traefik
    fqdn: prometheus.cluster.k8s
    gateway:
      name: edge
      ns: edge
      section: prometheus
