---
apiVersion: v1
kind: Namespace
metadata:
  name: "{{ ns }}"
  labels:
    {% if mesh == 'istio-ambient' %}
    istio.io/dataplane-mode: ambient
    {% elif mesh == 'istio-sidecar' %}
    istio-injection: enabled
    {% else %}
    {% endif %}
