apiVersion: v1
kind: Pod
metadata:
  name: mrmat-keycloak-test-connection
  labels:
    sidecar.istio.io/inject: "false"
  annotations:
    "helm.sh/hook": test
    prometheus.io/scrape: "false"
spec:
  restartPolicy: Never
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args: ["http://mrmat-keycloak-svc:80/"]
