apiVersion: v1
kind: Pod
metadata:
  name: mrmat-prometheus-test-connection
  labels:
    sidecar.istio.io/inject: "false"
  annotations:
    "helm.sh/hook": test
    prometheus.io/scrape: "false"
spec:
  restartPolicy: Never
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args: ["http://mrmat-prometheus-server:80"]
